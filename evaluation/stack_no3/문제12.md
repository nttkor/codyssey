1. LinkedList, CirculaList
   0. class Node(self,data):  (문제에는 없지만 구현해야함)
      1. self.data = data 
      2. self.next = None 구현해야함
   1. LinkedList 구현사항
      1. 문제에는 없지만 def __init__(self): self.head = None구현해야함
      2. insert(self,index,value) : 0<= index < last 범위 벗어나는 index, raise IndexError 처리
         1. self.head에 데이터가 없는 경우 있는 경우, index가 범위를 벗어나는 경우를 구분해서 return값및 에러 처리해야함
      3. delete(self,index) : index 범위 벗어 날경우 raise IndexError
      4. to_list(self) : 모든데이터를 리스트로 묶어서 리턴
      5. __len__(self)->int : 노드수 반환
      6. 문제에는 없지만 디버깅을 위해 disply를 만들자
   2. CircularList 구현 사항, last를 cursor라고 표현
      1. 문제에는 없지만 def __init__(self): self.last = None 구현으로 시작
      2. insert(self,value)
         1. 추가시 self.last가 비었는지, 있는지 마지막인지를 잘 구분해서 처리해야함
      3. delete(self,value)
         1. 지울때도 없을때, 하나 있을때, 없을때 등등 상황 처리를 잘해야함
      4. get_next(self) : 이건 last포인트를 last.next(즉 맨 처음)으로 바꾸고 출력하는것
         1. self.last = self.last.next, return self.last.data(이전의 head data)
      5. search(value) 아까 delete와 비슷 찾아지면 data return 하면 됨
      6. 문제에는 없지만 디버깅을 위해 disply를 만들자
      7. 구현후 모든 경우의 수 체크해보자
2. Stack 용량 10개로 고정
   1. push(self, value)->bool
   2. value 추가후 true 반환
   3. stack이 가득찬 경우 경고후 False 리턴 print("Stack is Full.")
   4. pop(self) : stack의 마지막을 꺼낸다 비어 있으면 print("Stack is empty.") 출력후 None 리턴
   5. empty(self) : 비어 있으면 True 아니면 False
   6. peek(self) : 스택의 맨위를 반환 비어 있으면 print("Stack is empty."), 출력후 None 리턴 
   7. Error Message
      1. assert False is None : None을 리턴해야 하는데 False가 리턴됨
      2. Atribute Error : 'Stack' object has no Empty
      3. FAILED test_stacked test_pop__lifo_and_underflow)message_assert_False
      4. test_empty_and_peek_behaviors_assert
      5. FAILED test_stacked_adt.py
      6. FAILED test_empty_and_peek_behaviors_assert_False is None
      7. FAILED test_capacity_invariant_after_failed_push_assert
      8. test_interleaved_operations_assert False is None
      9. test singly_insert_delete_basic_assert 3-2
      10. test_interleaved operations
      11. circular list에서 if len(self): return, current = self.cursor.next, while current:코드에서 next가 없다고 나옴 이건 def __len__(self): 구현을 return len(self)로 잘못한것 같음, return len(self.data), 또는 return count 로 해야 정상작동 될것 같음. 
3. 이진걸씨 
    1. 문제 2: Stack 구현하기
    스택은 LIFO(Last in, first out) 자료구조입니다.

    구현 사항:
    고정 크기(10개)의 스택을 구현하고, 항목 추가(push), 항목 삭제(pop),
    스택이 비었는지 확인(empty), 맨 윗부분 확인(peek) 기능을 제공하시오.

    스택이 가득 차거나 비었을 때는, 정해진 경고 메시지를 출력해야 함.

    이름: stack_adt.py
    구현할 Class명: "Stack"
    용량(capacity) = 10


    구현할 method
    push(self, value:any) -> bool:

    stack에 value를 추가하고, 성공 시 True 반환.
    stack이 가득 찬 경우: 아무것도 추가하지 않은 채로:
    print()로 "Stack is full." 출력
    False 반환


    pop(self) -> object|None:
    stack의 가장 마지막으로 넣은(맨 위) 항목을 '꺼내서' 반환함
    stack이 비어있으면:
    print()로 "Stack is empty." 출력
    return None

    empty(self) -> bool:
    -stack이 비어있으면 True, 아니면 False 반환

    peek(self) -> object|None:
    stack의 맨 위 항목을 삭제하지 않고 반환함
    stack이 비어있으면 
    print()로 "Stack is empty." 출력
    return None

    '''
    2. #나의 답안 (4/5)

'''python
class Node:
    def init(self, value):
        self.value = value
        self.next = None

class Stack:
    def init(self):
        self.count = 0
        self.head = None
        self.upper = None

    def push(self, value:any) -> bool:
        if self.count + 1 > 10:
            print("Stack is full.")
            return False
        else:
            new_node = Node(value)
            if self.count == 0:
                self.head = new_node
                self.upper = new_node
            else:
                self.upper.next = new_node
                self.upper = newnode
            self.count += 1
            return True

    def pop(self) -> object|None:
        if self.count == 0:
            print("Stack is empty.")
            return None
        if self.count == 1:
            value = self.upper # self.upper.value 로 잡을 것(값이니까)
            self.head = None
            self.upper = None
        else:
            value = self.upper # 여기도 self.upper.value로 잡기
            prev = self.head
            for  in range(self.count -1): # 하나 빼 버리면 prev는 None에 도달한다
                prev = prev.next           # -> range(self.count - 2)로 잡을 것
            self.head = prev #self.upper = prev인 것이다. head는 가만히 둘 것.
        self.count -= 1
        return value

    def empty(self) ->bool:
        if self.count == 0:
            return True
        else:
            return False

    def peek(self) -> object|None:
        if self.count == 0:
            print("Stack is empty.")
            return None
        else:
            val = self.upper.value
            return val

def main():
    print("PLAYGROUND!!")

if name == "main":
    main()